export interface Project {
    title: string
    description: string
    image: string
    tags: string[]
    features: string[]
    liveUrl?: string
    githubUrl?: string
    playStoreUrl?: string
    appStoreUrl?: string
} 